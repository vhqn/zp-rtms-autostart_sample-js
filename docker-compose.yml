version: '3.8'

services:
  # Backend API server (port 3001 - for ngrok)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: zoom-phone-backend
    ports:
      - "127.0.0.1:3001:3001"
    env_file:
      - ./.env
    environment:
      # Docker service name overrides (differ from .env localhost values)
      - FRONTEND_INTERNAL_URL=http://frontend:3000
      - RTMS_SERVER_URL=http://rtms:8080
    volumes:
      # Mount source code for hot reload
      - ./backend:/app:delegated
      # Prevent overwriting node_modules
      - backend-node-modules:/app/node_modules
      # Mount shared .env file
      - ./.env:/app/../.env:ro
    networks:
      - zoom-phone-network
    depends_on:
      - frontend
      - rtms

  # Frontend React dev server (port 3000 - internal only)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: zoom-phone-frontend
    # No external port mapping - accessed through backend proxy
    env_file:
      - ./.env
    environment:
      # Docker-specific overrides
      - REACT_APP_BACKEND_URL=/api
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
      - WDS_SOCKET_PORT=0
    volumes:
      # Mount source code for hot reload
      - ./frontend/src:/app/src:delegated
      - ./frontend/public:/app/public:delegated
      # Prevent overwriting node_modules
      - frontend-node-modules:/app/node_modules
    networks:
      - zoom-phone-network
    stdin_open: true
    tty: true

  # RTMS server (port 8080 - internal only, handles signaling/media WebSockets)
  rtms:
    build:
      context: ./rtms
      dockerfile: Dockerfile
    container_name: zoom-phone-rtms
    platform: linux/amd64
    # No external port mapping - accessed through backend
    env_file:
      - ./.env
    environment:
      # Docker service name overrides (differ from .env localhost values)
      - PORT=8080
      - BACKEND_URL=http://backend:3001
      - RTMS_DATA_DIR=/app/data
    volumes:
      # Mount source code for hot reload
      - ./rtms:/app:delegated
      # Prevent overwriting node_modules
      - rtms-node-modules:/app/node_modules
      # Persist audio and transcript data (override with RTMS_DATA_HOST_DIR)
      - ${RTMS_DATA_HOST_DIR:-./rtms/data}:/app/data:delegated
      # Mount shared .env file
      - ./.env:/app/../.env:ro
    networks:
      - zoom-phone-network

networks:
  zoom-phone-network:
    driver: bridge

volumes:
  frontend-node-modules:
  backend-node-modules:
  rtms-node-modules:
